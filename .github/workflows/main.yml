# .github/workflows/chromatic.yml

# Workflow name
name: 'Review on push'

# Event for the workflow
on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "master"

# List of jobs
jobs:
  chromatic-deployment:
    # Operating System
    runs-on: ubuntu-latest
    # Job steps
    steps:
      - name: Install dependencies
        run: echo "Install dependencies"
        
      - name: Publish to review
        run: echo "Published to review"
